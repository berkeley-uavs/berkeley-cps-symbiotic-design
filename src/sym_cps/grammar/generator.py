class Grid:
    nodes: list[list[list[str]]]
    adjacencies: dict[tuple, list[tuple]]


def generate_design(grammar_rules: dict | None = None,
                    size_grid: int | None = None) -> Grid:
    pass
